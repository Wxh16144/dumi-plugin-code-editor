{
  "name": "playground",
  "private": true,
  "scripts": {
    "build": "dumi build",
    "dev": "dumi dev",
    "prepare": "dumi setup",
    "start": "hard-load && npm run dev"
  },
  "devDependencies": {
    "hard-load":"latest",
    "dumi": "^2",
    "dumi-plugin-code-snippets": "^1.2.0",
    "dumi-plugin-code-editor": "workspace:*"
  }
}
